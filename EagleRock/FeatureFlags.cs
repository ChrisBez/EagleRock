namespace EagleRock
{
    /// <summary>
    /// Just a store of Feature flag strings to avoid hardcoding the strings all over the app
    /// </summary>
    public static class FeatureFlags
    {
        public const string RabbitMq = "RabbitMQ";
    }
}
